node_modules
.next
out
.env*.local
.vercel
coverage
playwright-report
test-results
next-env.d.ts
*.tsbuildinfo
public/images/source/
test-report.junit.xml

# WordPress migration data — content already extracted to content/ and public/images/
# Keep locally for re-running scripts but never commit raw export
wordpress-export/

# Test/dev screenshots
.playwright-mcp/

# Accidentally introduced test library
public/axe.min.js

# Local ruflo / ruvector MCP tooling artifacts (agent memory / vector DB)
agentdb.rvf
agentdb.rvf.lock
ruvector.db
